# Copie para .env e preencha. NUNCA suba o .env no GitHub.
PORT=3000
JWT_SECRET=TROQUE_POR_STRING_ALEATORIA_LONGA_64_CHARS
JWT_EXPIRES=7d

# URL base do Notícias Agrícolas — não altere
SCRAPE_BASE_URL=https://www.noticiasagricolas.com.br

# Intervalo de atualização em minutos (padrão: 30)
SCRAPE_INTERVAL_MIN=30

NODE_ENV=production
